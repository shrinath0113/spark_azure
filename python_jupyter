python3 
ctrl+Z


#BASHRC_FILE
sudo nano ~/.bashrc
export PATH=$PATH:~/.local/bin
export PYTHONPATH=$SPARK_HOME/python:$PYTHONPATH
export PYSPARK_DRIVER_PYTHON="jupyter"
export PYSPARK_DRIVER_PYTHON_OPTS="notebook"
export PYSPARK_PYTHON=python3
source ~/.bashrc


sudo apt install python3-pip

pip install jupyter

jupyter notebook

ctrl+Z

##for scala install java first

sudo apt-get install scala

scala -version

pip3 install  py4j

##inside spark/python>>>open terminal

jupyter notebook

create new file 
  import pyspark
  from pyspark.sql import SparkSession
  spark = SparkSession.builder.appName('TestingSpark').get0rCreate()
  print(spark)
